<Window x:Class="AntivirusUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Motor Antivirus - Lenguaje de Máquina" Height="550" Width="880"
        Background="Transparent" 
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CaptionHeight="30" UseAeroCaptionButtons="True"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="#66000000" BorderBrush="#444" BorderThickness="1" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0,4"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
            <TextBlock Text="MOTOR ANTIVIRUS" Foreground="#FFFFFF" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                </TextBlock.Effect>
            </TextBlock>
            <TextBlock Text="ESCÁNER DE MALWARE Y DESTRUCTOR x64" Foreground="#00FF41" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#66000000" CornerRadius="12" Padding="20" Margin="0,0,0,25"
                BorderBrush="#33ffffff" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.3" Color="Black"/>
            </Border.Effect>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox x:Name="txtRuta" Width="380" Height="40" FontSize="14" IsReadOnly="True" VerticalContentAlignment="Center" Margin="0,0,15,0"/>
                <Button x:Name="btnSeleccionar" Content="Elegir Carpeta" Width="110" Height="40" Background="#555555" Click="BtnSeleccionar_Click" Margin="0,0,10,0"/>
                <Button x:Name="btnEscanear" Content="ESCANEAR" Width="110" Height="40" Background="#2563EB" Click="BtnEscanear_Click" Margin="0,0,10,0"/>
                <Button x:Name="btnDestruir" Content="Destruir Archivo" Width="125" Height="40" Background="#DC2626" Click="BtnDestruir_Click"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Background="#99000000" CornerRadius="12" BorderBrush="#33ffffff" BorderThickness="1" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.4" Color="Black"/>
            </Border.Effect>

            <ListBox x:Name="lstResultados" Background="Transparent" BorderThickness="0"
                     Foreground="#00FF41" FontFamily="Consolas" FontSize="14"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
        </Border>
    </Grid>
</Window>